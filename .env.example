PORT=5000

# Local development (use localhost). On Render, set these in the Dashboard instead.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=trackingDB

# Set to true for most managed/cloud MySQL providers
DB_SSL=false

# Optional
JWT_SECRET=change_me
